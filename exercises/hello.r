"Hello"

3 * 09

ften <- seq(1, 10)

ften

matrix(ften, nrow = 2, ncol = 5) # same types

# data frame, different types
# same stuff for one dimension: list

df <- data.frame(
  a = 1:5,
  b = c("a", "b", "c", "d", "e"),
  c = c(TRUE, FALSE, TRUE, FALSE, TRUE)
)

df
